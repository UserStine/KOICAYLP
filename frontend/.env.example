# Local development only. Production uses the same-origin /api proxy.
VITE_API_URL=http://localhost:4000

# Vercel/server-side only. Set this on the FRONTEND project, not in React code.
# Example: https://your-koica-backend.vercel.app
BACKEND_URL=
